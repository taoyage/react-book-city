// import useSWR from 'swr';
// import fetch from '@/utils/request';

// import { IBookListData } from '@/pages/bookList/types';

// const getBookList = (url: string) => fetch.get(url);

// export const useBookList = (key: string, pageIndex: string | number = 1) => {
//   const url = `/api/v1/bookList${key}?pageIndex=${pageIndex}`;
//   return useSWR<IBookListData>(url, getBookList);
// };
